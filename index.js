// import { and, desc, gt, gte, lt, lte, not, or, ref, iff } from "./logic.js";
// import { intersect } from "./utils.js";

import "./logic2.js";

// const a = ref("a");
// const b = ref("b");

// const m = or(and(gt(a, 1), lt(a, 2)), and(gte(a, 3), lte(a, 4)));

// const app = iff(m, 1, 2);

// console.log(desc(app));

// console.log(intersect([1, 2, 3, 4], [2, 3, 4, 5, 6]));
